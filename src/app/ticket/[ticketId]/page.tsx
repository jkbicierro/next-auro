"use client";

export default function TicketScreen() {
    // react state variable

    // params

    // fetch ticket data dapat equal sa ticket_id or params
    // useEffect
    // try catch para error handling

    return (
        <>
            <main className="h-dvh flex flex-col items-center justify-center">
                {/* typescript/javascript mapping object */}
                <h1>Ticket</h1>
                <p>Ticket ID</p>
                <p>Ticket Details</p>
                <p>Ticket Status</p>
                <p>Ticket History</p>
                <p>Ticket Comments</p>
            </main>
        </>
    );
}
